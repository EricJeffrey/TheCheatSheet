#if !defined(RESPONSE_HELPER_HPP)
#define RESPONSE_HELPER_HPP

#include <string>

using std::string;

struct ResponseHelper {
    static constexpr char KEY_TOTAL_COUNT[] = "total";
    static constexpr char KEY_CODE_SEGMENTS[] = "codeSegments";
};

#endif // RESPONSE_HELPER_HPP
